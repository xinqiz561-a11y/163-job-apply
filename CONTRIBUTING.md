# Contributing

感谢贡献。提交前请确认：

1. 示例只使用虚构姓名、邮箱和岗位；
2. 不包含真实简历、手机号、授权码或发送日志；
3. `python skills/163-job-apply/scripts/test_skill.py` 通过；
4. 发送流程仍保持默认 dry-run、明确确认和收件人核验；
5. 新功能不能把 SMTP 接受误写成最终投递成功。

项目保持 163 专用范围。其他邮箱服务请另建 provider 设计，不要悄悄把
网页登录自动化或 OAuth 凭据塞进核心发送脚本。
